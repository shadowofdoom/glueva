{
  "name": "glueva",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "bin": {
    "glueva": "./bin/glueva"
  },
  "scripts": {
    "build": "bun build ./src/cli.ts --compile --no-compile-autoload-dotenv --no-compile-autoload-bunfig --outfile ./dist/glueva",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
